spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product_db
    username: postgres
    password: postgres

  jpa:
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

  cache:
    type: simple  # Use simple cache for local development

server:
  port: 8082

jwt:
  secret: local-development-secret-key-minimum-32-characters-long

logging:
  level:
    com.ecommerce.product: DEBUG
